Just paste this line into the project directory, and try to use SHA1
ex. Directory is Documents/project_name

MAC - 
keytool -list -v -keystore ~/.android/debug.keystore -alias androiddebugkey -storepass android -keypass android
OR 
inside the android folder in flutter
 ./gradlew signingReport 
